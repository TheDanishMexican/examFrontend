import { Fade } from '@mui/material'

export default function DisciplinesPage() {
    return (
        <Fade in={true} timeout={1000}>
            <div>
                <h1>Disciplines</h1>
            </div>
        </Fade>
    )
}
