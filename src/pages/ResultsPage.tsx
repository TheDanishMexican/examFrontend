import { Fade } from '@mui/material'

export default function ResultsPage() {
    return (
        <Fade in={true} timeout={1000}>
            <div>
                <h1>Results Page</h1>
            </div>
        </Fade>
    )
}
